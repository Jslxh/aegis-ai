# Base URL of the FastAPI backend (used by the dev proxy).
VITE_API_BASE_URL=/api
# Optional: direct backend origin (no proxy) — set to http://<host>:8000 when
# the frontend is served separately from the API.
# VITE_API_ORIGIN=
